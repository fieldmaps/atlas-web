import React from 'react';
import MapboxGlMap from '../components/mapbox-gl-map-index';

const IndexPage = () => {
  return (
    <div className="field-maps-flex-container">
      <MapboxGlMap />
    </div>
  );
};

export default IndexPage;
